<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/statistics"
    >
    <TextView
        android:id="@+id/tvStatisticsHeader"
        android:theme="@style/Base.Theme.NifferAndroid.HeaderTextAppearance"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/statistics"
        android:textSize="36sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/statistics" />
    <ru.niffer_android.views.StatisticsView
        android:id="@+id/statisticsView"
        android:layout_width="180dp"
        android:layout_height="180dp"
        app:textSize="40sp"
        app:lineWidth="16dp"
        app:color1="@color/green"
        app:color2="@color/red"
        app:color3="@color/orange"
        app:color4="@color/yellow"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvStatisticsHeader"
        />
    <LinearLayout
        android:id="@+id/categoryChipsContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        app:layout_constraintStart_toEndOf="@id/statisticsView"
        app:layout_constraintTop_toBottomOf="@id/tvStatisticsHeader"
        />

</androidx.constraintlayout.widget.ConstraintLayout>